# AGENTS.md — 项目规则

> AI 进入本项目必须先读这个文件。
> 第一份规矩来自 xuche(2026-09-16 立),这份优先级最高。
> 下面的「附录 A」是 Day 1 立的辅助规范,优先级次于本文件。

---

## 项目基本信息

- **项目名称**:vibe-coding-journey(七日 vibe coding 学习之旅)
- **工作区路径**:`C:\Users\xuche\vibe-coding-journey`
- **学习者**:xuche(中文沟通,零基础起步)
- **开始日期**:2026-09-16

---

## 一、任务边界(最重要)

1. 收到「今日任务清单」后,**这份清单就是今天唯一的任务范围**,只做清单里写的事。
2. 不做清单以外的工作:不建多余的文件、不写清单没提的功能、不顺手优化已有代码。你觉得可以改得更好,先跟我说一句,等我点头。
3. 不提前做以后的天数。只有我把某天的清单发给你、并明确说「现在开始做 Day X」,才允许动那天。
4. 拿不准是否在范围内,先问我,不要猜。

---

## 二、交互式推进

1. 收到清单后,先只给我今天的执行计划:按什么顺序做、每步怎么做、哪些要我自己动手。**写完停下,等我确认。**
2. 确认后**一次只做一个步骤**,绝不连着往下做。
3. 每步做完立刻停下,告诉我三件事:做了什么、改了哪些文件、怎么验证(附我能亲眼确认的结果)。
4. 然后**停下等我回复「进入下一板块」**。我没说这四个字,你就不要继续。
5. 「进入下一板块」只对当前这一步有效,不代表可以一路做到收尾。

---

## 三、人机分工

1. 需要我人工操作的(注册账号、点按钮、装软件、截图等):不要替我做,也不要假装已完成。只告诉我:这步在干什么、一二三步怎么做、做完我该看到什么、容易踩什么坑,然后停下等我。
2. 需要你生成的内容(写文档、建文件、写代码):**等我说了再做**,不要在我刚说「开始吧」时就把整天的东西生成出来。
3. 需要拍板的地方(选方案、起名字、定风格),把选项和各自代价列给我选,不要自己决定。
4. 验证结果要我能亲眼确认(页面能打开、文件能看到、命令有输出),不接受「已经完成了」这种口头结论。

---

## 四、讲清楚环节(我是来学的)

1. 每个步骤开始前,先告诉我:它在整个作品里是什么位置、为什么现在做、做完会变成什么样。
2. 每步用「做了什么 → 结果 → 为什么」讲明白,不要只给结果。
3. 我不懂的概念(命令、报错、术语、配置),一两句话解释清楚,不要默认我会。
4. 报错时先告诉我错在哪、可能什么原因、打算怎么修,我同意后再动手。不要输出教程式长文。

---

## 五、Git 与提交

1. 我授权你代为执行 git 提交和推送,但**提交前必须先列出**本次会改动哪些文件、每个文件属于哪天的任务,等我确认后再提交。
2. commit 信息格式:`Day X｜一句话说明`。
3. 密钥、`.env`、数据库连接串永远不进代码、不进提交。
4. 一天的任务全部做完、我核对完文件清单之后才提交,中途不要边做边提交。

---

## 六、当日收尾

1. 我说「今天做完了」之后,请你逐条对照清单里的「完成标准」,输出一张表:完成标准 / 状态(已完成·部分完成·未完成)/ 证据(文件、页面、命令输出)。
2. 核对必须诚实,没做完、做了一半、跳过没做的,直接标出来并说明原因,不要为了凑「全部完成」而模糊处理。
3. 有未完成项时,告诉我还差什么、接不接着做,**由我决定**,不要自己往下做。
4. 全部完成时,按清单的「收尾指令」整理当天成果,提醒我该交哪张截图、该在群里回什么。

---

## 七、出问题的时候

1. 我要求撤销时,用安全回退(`git revert`,保留历史),禁止 `git reset --hard` 和强制推送。
2. 回退前先和我确认恢复到哪个提交,回退后列出当前状态让我核对。
3. 同一个操作反复失败时停下来报告,说清楚试了什么、报什么错、你判断的原因,不要循环重试。

---

## 附录 A:Day 1 立的辅助规范(已并入主规矩,这里仅作存档)

- 沟通用简体中文,术语带中文翻译。
- 技术概念先一句话定义,再打生活化比方。
- 作业全部放在 `C:\Users\xuche\vibe-coding-journey\`,不散落桌面。
- 文件名用英文/拼音,空格换 `-`(例:`day-1-notes.md`)。
- 截图放 `screenshots/` 子目录,按日期命名。
- 能用 Bash / PowerShell 跑的事不让我手动找文件管理器。
- 危险操作(`rm`、`del`、覆盖、清空)前先列影响范围,等我确认。
- 依赖装到项目 `node_modules`,不要全局 `npm install -g`。
- Git Bash 走 D 盘路径要加 `MSYS_NO_PATHCONV=1` 并用 Windows 风格路径(`D:/...`)。
- 卡住先跳过,记到待办,不浪费一晚上死磕一项。

---

## 附录 B:Day 1 验收记录

| 项目 | 状态 | 证据 |
| --- | --- | --- |
| WorkBuddy 已就绪 | ✅ | 当前会话 |
| Git 2.47.1 | ✅ | `git --version` |
| Node.js 22.22.2 | ✅ | `node -v` 输出落到 `node-check.txt` |
| GitHub 账号 | ✅ | 用户本人 21:46 完成注册 |
| 工作区文件夹 | ✅ | `C:\Users\xuche\vibe-coding-journey\` |
| AGENTS.md | ✅ | 本文件 |

---

> 📌 每天开工前先读本文件。规矩变了就改,改完告诉 AI。

---

## 八、我的个人规则(Day 6 追加)

1. **临时文件不入库**:`node-check.txt`、`vscode-check.txt` 这类验证残留**只在本机**,不入 `.gitignore` 时要单独提醒我,避免哪天无意中推到 GitHub。
2. **截图归位**:截图必须存在 `C:\Users\xuche\vibe-coding-journey\screenshots\`,文件名格式 `dayN-场景.png`(例:`day6-agents-md.png`),不写桌面、不用系统默认名。
3. **前端先看再交**:HTML/CSS/JS 改完后,**先打开浏览器亲眼确认页面正常再提交**,提交信息里附一句"我看到 XX"作为证据,防止"提交成功但页面空白"。